{
  "name": "@mage-knight/ui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'UI build not yet configured'",
    "test": "echo 'UI tests not yet configured'",
    "lint": "echo 'UI lint not yet configured'"
  },
  "dependencies": {
    "@mage-knight/core": "workspace:*"
  }
}
